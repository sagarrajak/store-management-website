/**
 * Created by SAGAR on 1/21/2017.
 */
var  mongoose = require('mongoose');
var employee = require('../../schemas/retailer');

module.exports = function (app,express){
     var api = express.Router();
        //todo  function to sort by name
         //todo to find all product associated with the retailer
      return api;
}
